# =============================================================================
# Host Runner Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Wolf (Games on Whales) Configuration
# -----------------------------------------------------------------------------

WOLF_INTERNAL_MAC=00:11:22:33:44:55

# -----------------------------------------------------------------------------
# Host Info Service Configuration
# -----------------------------------------------------------------------------

# Port for the host info service
HOST_INFO_PORT=3001

# Thresholds for determining if the system is busy
# GPU utilization threshold (percentage)
GPU_UTIL_THRESHOLD=50

# GPU memory threshold (percentage)
GPU_MEM_THRESHOLD=25

# CPU utilization threshold (percentage)
CPU_UTIL_THRESHOLD=60

# Percentage of samples that must exceed thresholds to be considered busy
BUSY_THRESHOLD_PERCENT=60

# -----------------------------------------------------------------------------
# Idle Inhibitor Configuration
# -----------------------------------------------------------------------------

# Idle timeout in seconds (how long to wait after last activity before allowing sleep)
# Default: 300 seconds (5 minutes)
IDLE_TIMEOUT=300

# What to inhibit (systemd-logind format)
# Options: sleep, idle, sleep:idle, shutdown
INHIBIT_WHAT=sleep

# Reason shown in systemd-inhibit --list
INHIBIT_WHY=Wolf streaming session active

# Delay before reconnecting to SSE stream (seconds)
RECONNECT_DELAY=5

# -----------------------------------------------------------------------------
# Tailscale Configuration
# -----------------------------------------------------------------------------

# Tailscale authentication key (REQUIRED)
# Generate one at: https://login.tailscale.com/admin/settings/keys
TS_AUTHKEY=

# Hostname for this node in Tailscale network
TAILSCALE_HOSTNAME=cloudgaming
