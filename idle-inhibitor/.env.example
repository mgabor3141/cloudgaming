# SSE endpoint configuration
SSE_URL=http://localhost/api/v1/events
UNIX_SOCKET=/var/run/wolf/wolf.sock

# Idle timeout in seconds (default: 300 = 5 minutes)
IDLE_TIMEOUT=300

# What to inhibit (systemd-logind format)
# Common values: sleep, idle, sleep:idle, shutdown
WHAT=sleep:idle

# Reason shown in systemd-inhibit --list
WHY=Wolf streaming session active

# Reconnection delay in seconds
RECONNECT_DELAY=5

